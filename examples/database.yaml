id: component.database
name: Database
category: component
description: A persistent structured data store.
capabilities:
  - persistent-storage
  - structured-query
constraints:
  - stateful
mappings:
  aws: rds
  azure: azure-sql
  gcp: cloud-sql
tags:
  - data
  - storage
